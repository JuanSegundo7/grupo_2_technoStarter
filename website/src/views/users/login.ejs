<!DOCTYPE html>
<html lang="en">

<%-include("../partials/head",{"css":["header", "login", "footer"]})%>

<body>
    <%-include("../partials/header")%> 
    <main>
        <form action="/" method="POST" id="form-principal">
            <fieldset>
                <% if (locals.errors) {
                    errors.forEach(error => { %>
                        <p class="mensaje-error"><%= error[i].msg %></p>
                <%   });
                } 
                %> 
                <h3 class="margin-body">Bienvenido devuelta!</h3>
                <label for="nombreUsuario" class="margin-body">Direccion de correo electronico</label>
                <input type="text" placeholder="E-mail" id="email" name="email">
            </fieldset>
            <fieldset>
                <label for="contraseña" class="margin-body">Contraseña</label>
                <input type="password" placeholder="Contraseña" id="clave" name="clave">
            </fieldset>
            <fieldset>
                <input type="radio" name="recordarme" id="recordarme">
                <label for="recordarme">Recordarme</label>
            </fieldset>
            <fieldset id="flex-button">
                <button type="submit">Iniciar sesión</button>
                <button type="reset">Resetear</button>
            </fieldset>
            <fieldset class="olvidarContraseña" id="flex-password">
                <p>¿Te olvidaste la contraseña?</p>
                <p>Recuperala<a href="/">aca</a></p>
            </fieldset>
            <fieldset class="olvidarContraseña" id="flex-password">
                <p>¿No estas registrado?</p>
                <p>Unite<a href="/usuario/registrarse">aca</a></p>
            </fieldset>
        </form>
    </main>
    <%- include("../partials/footer")  %> 
    </body>
    </html>
